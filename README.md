# Mule_Database_connector

1.	Expose an API in Mule (/create, /update, /delete, /select, /bulkinsert)
2.	Try to install Mysql DB server
3.	Take your own request
4.	For each operation do the CURD operation in DB

